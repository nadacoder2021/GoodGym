{
  "name": "developer-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --names '[frontend],[backend:build],[backend:server]     ' --prefix \"| {name} |\" --prefix-colors blue,green,red  \"cd frontend && yarn start\" \"cd backend && yarn build\" \"cd backend && yarn start\""
  },
  "dependencies": {
    "@types/node": "^14.14.10",
    "concurrently": "~5.3.0",
    "wait-on": "^5.2.0"
  }
}
